---
"vscode-abap-remote-fs": minor
---

Add a configurable write policy (`abapfs.writePolicy.*`) that restricts creating, changing, deleting, activating and editing text elements of SAP objects by connection, package, object name and type. Applies to GitHub Copilot, the MCP server and manual actions; MCP requests are never offered the "Allow once" confirmation.
